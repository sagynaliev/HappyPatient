import {Link,NavLink,useNavigate} from 'react-router-dom';import {ReactNode,useState} from 'react';import {useAuth,roleLabel} from '../context/AuthContext';
export function Layout({children}:{children:ReactNode}){const {user,logout}=useAuth();const nav=useNavigate();const [open,setOpen]=useState(false);return <div className="app-shell"><header className="topbar"><Link className="brand" to="/"><span className="brand-mark">✚</span><span>Happy<span>Patient</span></span></Link><button className="menu-button" onClick={()=>setOpen(!open)} aria-label="Toggle menu">☰</button><nav className={open?'nav open':'nav'}><NavLink to="/search" onClick={()=>setOpen(false)}>Find care</NavLink>{user&&<NavLink to="/dashboard" onClick={()=>setOpen(false)}>Dashboard</NavLink>}{user?.role==='ADMIN'&&<NavLink to="/admin" onClick={()=>setOpen(false)}>Admin</NavLink>}{user?<><span className="user-chip">{user.firstName} · {roleLabel(user.role)}</span><button className="button button-outline small" onClick={()=>{logout();nav('/login')}}>Sign out</button></>:<><Link className="button button-outline small" to="/login">Sign in</Link><Link className="button small" to="/register">Get started</Link></>}</nav></header><main>{children}</main><footer><span>© 2026 HappyPatient</span><span>Care that puts you first.</span></footer></div>}
