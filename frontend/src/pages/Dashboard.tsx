import {Link} from 'react-router-dom';import {useAuth,roleLabel} from '../context/AuthContext';export default function Dashboard(){const {user}=useAuth();if(!user)return null;return <div className="page dashboard"><div className="welcome"><div><p className="eyebrow">Your dashboard</p><h1>Good to see you, {user.firstName}.</h1><p className="lead">Here’s what’s happening with your care.</p></div><div className="profile-avatar">{user.firstName[0]}{user.lastName[0]}</div></div><div className="stat-grid"><div className="stat"><span>Account type</span><strong>{roleLabel(user.role)}</strong><small>Active account</small></div><div className="stat"><span>Profile</span><strong>Complete</strong><small>Keep your details updated</small></div><div className="stat"><span>Next step</span><strong>Find care</strong><small>Discover your care team</small></div></div><div className="dashboard-grid"><section className="panel"><div className="panel-title"><h2>Find your care team</h2><span>⌕</span></div><p>Explore qualified doctors and specialists who are ready to help.</p><Link className="button" to="/search">Browse doctors →</Link></section><section className="panel soft"><div className="panel-title"><h2>Your profile</h2><span>◎</span></div><p>{user.email}<br/>{user.phone||'Add a phone number to your profile'}</p><button className="button button-outline">Edit profile</button></section></div></div>}
